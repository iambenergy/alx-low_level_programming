#include "main.h"

/**
 * main - Program for a function to prints the numbers,
 * from 0 to 9, followed by a new line.
 *
 * Return: Always 0 when successful
 */

int main(void)
{
	print_numbers();
	return (0);
}
